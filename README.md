# Installation necessaire
```
sudo apt update
sudo apt install build-essential nasm grub-pc-bin xorriso qemu-system-x86
```
# Architecture projet
```
mini-os/
├── boot/
│   └── grub.cfg
├── kernel/
│   └── kernel.c
├── linker.ld
└── Makefile
```


